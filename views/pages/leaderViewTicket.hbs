{{> viewTicket_navbar}}

<link rel="stylesheet" href="/css/pages/secretaryViewTicket.css">

<main class="ticket-details-shell">
    <div class="ticket-header-card">
        <div class="left-column">
            <p class="ticket-id">TICKET ID: {{ticket_id}}</p>
            <h1>{{category_name}}</h1>
        </div>
        <div class="right-column">
            <div class="ticket-header-actions">
                <span class="status-display">Κατάσταση:</span>
                <span class="status {{ticket.status.className}}">
                    <span class="dot"></span> {{ticket.status.label}}
                </span>
            </div>
        </div>
    </div>

    <section class="ticket-layout">
        <aside class="ticket-column left-column">
            {{>studentCard}}
        </aside>

        <section class="ticket-column center-column">
            <article class="detail-card request-card">
                <h2>Αρχικό Αίτημα Φοιτητή</h2>

                <div class="detail-block">
                    <span class="detail-label">ΘΕΜΑ</span>
                    <p>{{firstMessage.message_subject}}</p>
                </div>

                <div class="detail-block">
                    <span class="detail-label">ΠΕΡΙΓΡΑΦΗ ΑΙΤΗΜΑΤΟΣ</span>
                    <p>{{firstMessage.message_description}}</p>
                </div>

                <div class="detail-block">
                    <span class="detail-label">ΕΠΙΣΥΝΑΠΤΟΜΕΝΑ ΑΡΧΕΙΑ ΦΟΙΤΗΤΗ</span>
                    {{>fileList attachments=firstMessageAttachments}}
                </div>
            </article>

            <article class="detail-card history-card">
                <h2>Ιστορικό Επικοινωνίας</h2>
                {{>msgHistory messages=messages}}
            </article>
        </section>

        <aside class="ticket-column right-column">
            <article class="detail-card action-card">
                <h2>Ενέργειες Προϊσταμένου</h2>

                <form id="replyForm" method="post" enctype="multipart/form-data"
                    action="/tickets/secretary-view-ticket/ticket/{{ticket_id}}/reply">

                    <div class="action-block">
                        <label for="replyText">ΑΠΑΝΤΗΣΗ ΣΤΟΝ ΦΟΙΤΗΤΗ</label>
                        <textarea id="replyText" rows="5" name="replyText"
                            placeholder="Γράψτε την απάντησή σας στον φοιτητή..."></textarea>
                    </div>

                    <div class="action-block">
                        <label for="status">ΑΛΛΑΓΗ ΚΑΤΑΣΤΑΣΗΣ</label>
                        <select id="status" name="status" class="form-control" data-current="{{ticketStatusRaw}}" data-allow-closed-edit="1">
                            <option value="in_progress">Σε Εξέλιξη</option>
                            <option value="pending">Σε Αναμονή</option>
                            <option value="resolved">Ολοκληρωμένο</option>
                            <option value="closed">Κλειστό</option>
                        </select>
                    </div>

                    <div class="card attachments-card">
                        <!-- Drag & Drop Zone -->
                            <span class="detail-label">ΕΠΙΣΥΝΑΨΗ ΑΡΧΕΙΟΥ</span>
                            <div class="file-upload-zone" id="fileUploadZone">
                                    <img class="upload-icon" src="/images/upload-cloud-icon.svg" height="87" width="100" alt="">
                                    <p class="upload-text">Σύρετε αρχεία ή κάντε κλικ για επιλογή</p>
                                    <small class="upload-hint">Υποστηρίζονται PDF, DOC, JPG, PNG (max 10MB)</small>

                                    <input type="file" id="fileInput" name="files" multiple accept=".pdf,.doc,.docx,.jpg,.jpeg,.png"
                                        class="file-input">
                                </div>

                            <!-- File List -->
                            <div class="file-list " id="fileList"></div>

                            <!-- Error Message -->
                            <div class="file-error-message hidden" id="fileError"></div>
                        {{>submitWrap buttonType="submit" buttonText="Αποστολή Απάντησης / Αλλαγή Κατάστασης"}}
                    </div>
                </form>

                <form class="assign-form" method="post" action="/tickets/assign/{{ticket_id}}" style="margin-top: 20px;">
                    <div class="action-block">
                        <h2>Ανάθεση του αιτήματος σε :</h2>
                        {{!-- <label for="leaderSecretarySelect">Ανάθεση του αιτήματος σε</label> --}}
                        <select id="leaderSecretarySelect" name="secretary_id" class="form-control" required>
                            <option value="{{leaderSecretaryId}}" selected>{{leaderDisplayName}}</option>
                            {{#each secretaries}}
                                <option value="{{this.secretary_id}}">{{this.displayName}}</option>
                            {{/each}}
                        </select>
                        <div class="card attachments-card">
                        {{>submitWrap buttonType="submit" buttonText="Ανάθεση"}}
                        </div>
                    </div>

                    
                </form>
            </article>
        </aside>
    </section>
</main>
<script src="/js/partials/fileAttachment.js" defer></script>
<script src="/js/pages/viewTicket.js" defer></script>
